spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: ${DB_PASSWORD:backend}
    url: jdbc:postgresql://${DB_HOST:localhost:5432}/${DB_NAME:backend-db}
    username: ${DB_USERNAME:backend}
  devtools:
    add-properties: false
